import { Button } from '@mui/material';
import { BaseLayoutPage } from '../../shared/layouts';

export const Dashboard = () => {
  
  return (
    <BaseLayoutPage title='Initial Page'>
      <Button variant="contained">Test</Button>
    </BaseLayoutPage>
  );
};